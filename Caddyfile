# Replace monitor.yourdomain.com with your actual domain
# Replace 192.168.1.x with the IP address of the machine running Docker

monitor.yourdomain.com {
    # API backend
    reverse_proxy /api/* 192.168.1.x:3001

    # Web frontend
    reverse_proxy 192.168.1.x:3000
}